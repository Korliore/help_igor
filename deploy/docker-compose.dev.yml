version: '3.9'

services:
  api:
    ports:
      # Exposes application port.
    - "8000:8000"
    - "5432:5432"
    build:
      target: dev
    volumes:
      # Adds current directory as volume.
    - .:/app/src/
    environment:
      # Enables autoreload.
      SRC_RELOAD: "True"
